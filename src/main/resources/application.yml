server:
  port: 8090

spring:
  datasource:
    username: root
    password: Masubi98%
    url: jdbc:mysql://localhost:3306/PrimeClimateCare
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format-sql: true

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
      file-size-threshold: 2KB